# Archived documentation

This directory keeps historical design, migration, and refactoring records that
are useful for context but are not current operating instructions.

Use active guides in `docs/`, current source code, and generated release
artifacts for present-day behavior.
